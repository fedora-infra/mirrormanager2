#!/usr/bin/python
__requires__ =  'TurboGears[future]'
import pkg_resources

CONFFILE='##CONFFILE##'
PROGRAMDIR='##PROGRAMDIR##'

from turbogears import config, update_config, start_server
import cherrypy
cherrypy.lowercase_api = True
from os.path import *
import sys

if len(sys.argv) > 1:
    update_config(configfile=sys.argv[1] ,
                  modulename="mirrors.config")
elif exists(join(dirname(__file__), "setup.py")):
    update_config(configfile="dev.cfg",modulename="mirrors.config")
else:
    update_config(configfile=CONFFILE, modulename="mirrors.config")

sys.path.append(PROGRAMDIR)

from mirrors.controllers import Root

start_server(Root())
