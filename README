This is the top level of the Fedora Mirror Management system.

Directories include:

server/ 
    - The TurboGears web site and database schema

mirrorlist-server/ 
    - The daemon process that has the cached data, and fork()s for every 
      request

client/ 
    - Code and sample configuration files that are run on mirror hosts

fedora-test-data/ 
    - Something to get started

